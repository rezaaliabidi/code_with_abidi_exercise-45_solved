//Exercise 45 Q 12
let members: string[] = [`sohail`, `nabeel`, `hira`, `sara`];
let message: string = `would you like to learn typescript with:`;
for(let i=0; i<members.length; i++){
    console.log(message + members[i]);
}